using System;

namespace communication.dds.opensplice.adapters.configuration
{
    public static class Constants
    {
        public static readonly TimeSpan RequestTimeout = TimeSpan.FromSeconds(5);
    }
}